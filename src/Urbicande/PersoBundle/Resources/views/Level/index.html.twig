{% extends '::base.html.twig' %}

{% block title %} {{ parent() }} - Niveaux de Personnage {% endblock %}

{% block body %}

<div class="row">
  <h1 class="ten columns">
    Les niveaux de personnage
  </h1>
  <div class="two hide-for-small columns">
    <div class="button gray large-header">
      <a href="{{ path('create_level') }}">Créer un niveau</a>
    </div>
  </div>
</div>


<table class="records_list datatable">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Description</th>
      <th>Nombre de personnages</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  {% for level in levels %}
      <tr>
        <td><a title="Voir" href="{{ path('level_by_id', { 'id': level.id }) }}">{{ level.name }}</a></td>
        <td>{% autoescape false %}{{ level.description|slice(0,60) }}{% endautoescape %}</td>
        <td>{{ level.players|length }}</td>
        <td>
          <ul class="button-group even two-up">
            <li>
              <div class="button green">
                <a title="Éditer" href="{{ path('level_edit', { 'id': level.id }) }}"><i class="icon-pencil"></i></a>
              </div>
            </li>
            <li>
              <div class="button red">
                <a title="Supprimer" href="{{ path('level_delete', { 'id': level.id }) }}"><i class="icon-remove-sign"></i></a>
              </div>
            </li>
          </ul>
        </td>
      </tr>
    </a>
  {% endfor %}
  </tbody>
</table>

{% endblock body %}
