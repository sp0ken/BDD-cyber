<html>
  <head></head>
  <body>
    Salut {{commentee}},

    {{commenter}} vient de laisser un commentaire sur <a href="{{url('intrigue_by_id', {'id': comment.intrigue.id})}}">{{comment.intrigue.name}}</a>.<br><br>
    <blockquote>
      {% autoescape false %}
        {{comment.comment}}
      {% endautoescape %}
    </blockquote>

    Signé Le Robot de Jeff,
  </body>
</html>
