{% extends '::base.html.twig' %}

{% block title %} {{ parent() }} - {{ type.name}} {% endblock %}

{% block body %}

<div class="row">
  <h1 class="ten columns">
    {{ type.name }}
  </h1>
  <div class="two hide-for-small columns">
    <div class="button gray large-header">
      <a href="{{ path('object_type_edit', {'id': type.id}) }}">Modifier</a>
    </div>
  </div>
</div>

<div class="spacer"></div>

<div class="row">
  {% autoescape false %}
    <h3 class="twelve columns">Description</h3>
    <div class="twelve columns">{{type.description}}</div>
  {% endautoescape %}
</div>

<div class="spacer"></div>

<div class="row">
  <h3 class="twelve columns">Liste des objets <small>({{type.objects|length}} objets)</small></h3>
  <table class="records_list datatable">
    <thead>
      <tr>
        <th>Nom</th>
        <th>Position de départ</th>
        <th>Référent</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
    {% for object in type.objects %}
      <tr>
        <td><a title="Voir" href="{{ path('object_by_id', { 'id': object.id }) }}">{{ object.name }}</a></td>
        <td>{{ object.origin }}</td>
        <td>{{ object.creator }}</td>
        <td>
          <ul class="button-group even two-up">
            <li>
              <div class="button gray">
                <a href="{{ path('object_by_id', { 'id': object.id }) }}"><i class="icon-eye-open"></i></a>
              </div>
            </li>
            <li>
              <div class="button green">
                <a href="{{ path('object_edit', { 'id': object.id}) }}"><i class="icon-pencil"></i></a>
              </div>
            </li>
          </ul>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endblock body %}